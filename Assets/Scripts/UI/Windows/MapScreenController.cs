namespace Game.UI.Windows
{
    /// <summary>
    /// The map full-screen. Actual map rendering/markers are a follow-up —
    /// this exists to prove a new catalog entry needs no changes to
    /// WindowManager, the hotkey router, or the tab bar.
    /// </summary>
    public class MapScreenController : SimpleWindow
    {
    }
}
