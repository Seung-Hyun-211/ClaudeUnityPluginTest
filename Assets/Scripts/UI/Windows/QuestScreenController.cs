namespace Game.UI.Windows
{
    /// <summary>
    /// The quest log full-screen ("임무 확인"). Actual quest list content is a
    /// follow-up — this exists to prove a fourth catalog entry needs no
    /// changes to WindowManager, the hotkey router, or the tab bar.
    /// </summary>
    public class QuestScreenController : SimpleWindow
    {
    }
}
