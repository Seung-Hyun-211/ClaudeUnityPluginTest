namespace Game.UI.Windows
{
    /// <summary>
    /// The settings full-screen panel. Content (graphics/audio/controls tabs)
    /// is a follow-up — this establishes it as a second full-screen window
    /// that shares WindowManager's exclusivity with the inventory.
    /// </summary>
    public class SettingsScreenController : SimpleWindow
    {
    }
}
